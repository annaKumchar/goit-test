import { CardHeader, HeaderDetails, HeaderLogo } from './Header.styled';

export const Header = () => {
  return (
    <CardHeader>
      <HeaderLogo/>
      <HeaderDetails />
    </CardHeader>
  );
};
